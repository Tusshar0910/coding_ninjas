#include<iostream>
using namespace std;

int main() {
	int n;
	cin >> n;
	bool even = n % 2 != 0 ? false : true;
	if (!even)n--;
	int sum = 0;
	while (n >= 2) {
		sum += n;
		n -= 2;
	}
	cout << sum;
} 
